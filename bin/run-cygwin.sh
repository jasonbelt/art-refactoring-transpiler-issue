#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
cd $SCRIPT_HOME

# Uncomment the following to prevent terminal from closing if app crashes
#PREVENT_CLOSE="; bash -i"

cygstart mintty /bin/bash "cygwin-build/TempSensor_i_tcp_tempSensor_App.exe${PREVENT_CLOSE}" &
cygstart mintty /bin/bash "cygwin-build/TempControl_i_tcp_tempControl_App.exe${PREVENT_CLOSE}" &
cygstart mintty /bin/bash "cygwin-build/Fan_i_tcp_fan_App.exe${PREVENT_CLOSE}" &
read -p "Press enter to initialise components ..."
cygwin-build/Main.exe
read -p "Press enter again to start ..."
cygwin-build/Main.exe