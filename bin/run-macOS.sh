#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
cd $SCRIPT_HOME

# Uncomment the following to prevent terminal from closing if app crashes
#PREVENT_CLOSE="; bash -i"

open -a Terminal "macOS-build/TempSensor_i_tcp_tempSensor_App${PREVENT_CLOSE}" &
open -a Terminal "macOS-build/TempControl_i_tcp_tempControl_App${PREVENT_CLOSE}" &
open -a Terminal "macOS-build/Fan_i_tcp_fan_App${PREVENT_CLOSE}" &
read -p "Press enter to initialise components ..."
macOS-build/Main
read -p "Press enter again to start ..."
macOS-build/Main